ToDos
=====
 - fix the TimeStamp bug since trying to "enforce" UTC leads to errors.
 - produce correct JSON files. The current files are "pseudo-JSON"
 - replace the Jackson DataMapper since it's slow.
 - add formatting (and parsing) options
 - add some missing unit tests